Plain-text contact link (email, GitHub, LinkedIn). Body colour by default; global `a:hover` rule turns it gold with an underline. No icon-soup — text only.

```jsx
<ContactLink href="mailto:kim@mikaelleon.dev">kim@mikaelleon.dev</ContactLink>
```
