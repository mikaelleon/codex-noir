Skill / tech-stack chip. Mono type is reserved for exactly this use (plus index numbers) — never headings or body text.

```jsx
<Tag>Firebase</Tag>
```

No progress bars, no ratings, no fill percentage — skills render as a clean tag grid only.
