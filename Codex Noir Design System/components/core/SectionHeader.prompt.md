Section-level heading — Cinzel, small caps, wide tracking. `sparkle` adds the single four-point glyph marker (use once per section max, not as decoration).

```jsx
<SectionHeader sparkle>Projects</SectionHeader>
```
