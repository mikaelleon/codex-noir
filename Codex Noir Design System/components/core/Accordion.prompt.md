Single-open FAQ accordion — hairline dividers, Cinzel question labels, Garamond answers, plain mono +/− indicator (no icon font).

```jsx
<Accordion items={[{ q: 'What stack do you build with?', a: 'React, Node, and Firebase, with Git for everything.' }]} />
```
